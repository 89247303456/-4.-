"""Даны два неупорядоченных набора целых чисел (может быть, с 
повторениями). Выдать без повторений в порядке возрастания все те 
числа, которые встречаются в обоих наборах.
Пользователь вводит 2 числа. n — кол-во элементов первого множества.
m — кол-во элементов второго множества. Затем пользователь вводит 
сами элементы множеств."""

import time
import cProfile

def find_common_elements(set1, set2):
    # Создаем словарь для хранения уникальных чисел из обоих наборов
    result_dict = {}
    
    # Используем пересечение множеств для нахождения общих элементов
    common_elements = set1 & set2
    
    # Добавляем общие элементы в словарь
    for element in common_elements:
        result_dict[element] = True
    
    # Возвращаем отсортированный список общих элементов
    return sorted(result_dict.keys())

# Вводим количество элементов первого и второго множеств
n = int(input("Введите количество элементов первого множества: "))
m = int(input("Введите количество элементов второго множества: "))

# Вводим элементы первого множества
set1 = set()
print("Введите элементы первого множества:")
for i in range(n):
    element = int(input())
    set1.add(element)

# Вводим элементы второго множества
set2 = set()
print("Введите элементы второго множества:")
for i in range(m):
    element = int(input())
    set2.add(element)

# Запускаем профилирование
pr = cProfile.Profile()
pr.enable()

# Вызываем функцию для нахождения общих элементов
common_elements = find_common_elements(set1, set2)

# Останавливаем профилирование
pr.disable()

# Выводим общие элементы
print("Общие элементы, найденные в обоих множествах:")
for element in common_elements:
    print(element)

# Выводим информацию о профилировании
pr.print_stats()
